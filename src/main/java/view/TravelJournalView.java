package view;

import javafx.scene.layout.VBox;

public class TravelJournalView {
    private VBox root;

    public TravelJournalView() {
        root = new VBox();
        // Initialize UI components (input fields, buttons, etc.)
    }

    public VBox getRoot() {
        return root;
    }
}
